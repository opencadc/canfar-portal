apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canfar-web-portal.fullname" . }}-test-connection"
  labels:
    {{- include "canfar-web-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canfar-web-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
