apiVersion: v1
kind: Service
metadata:
  name: {{ include "canfar-web-portal.fullname" . }}
  labels:
    {{- include "canfar-web-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "canfar-web-portal.selectorLabels" . | nindent 4 }}
